import java.util.*;
class VectorClass
{
	public static void main(String arg[])
	{
      VectorClass v=new VectorClass(3,2);
     /* System.out.println(" Initial size : "+v.size());
      System.out.println(" Initial capacity : "+v.capacity());
      v.addElement(new Integer(1));
       v.addElement(new Integer(2));
        v.addElement(new Integer(3));
         v.addElement(new Integer(4));
         System.out.println(" capacity after four additios : "+v.capacity());
          v.addElement(new Double(45.6));
          System.out.println(" Current capacity : "+v.capacity());
           v.addElement(new Float(746.765));
           System.out.println(" Current capacity : "+v.capacity());
           System.out.println(" First element"+(Integer)v.firstElement());
            System.out.println(" Last element"+(Integer)v.lastElement());
            if(v.contains(new Integer(3)))
            	System.out.println(" Vector contain 3.");
            Enumeration vEnum=v.elements();
            System.out.println(" Elements in vector : ");
            while(vEnum.hasMoreElements())

            	System.out.pritln(vEnum.nextElement()+" ");
            System.out.println();*/
	}
}